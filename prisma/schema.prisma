generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./wordle.db"
}

model DailyWord {
  id    Int    @id @default(autoincrement())
  date  String
  mode  String
  word  String
  words String

  @@unique([date, mode], name: "date_mode")
}